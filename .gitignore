node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example
.DS_Store
.vercel
coverage/
/var/waitlist.jsonl

# Skills d'agents installées via `npx skills add`.
# Traitées comme node_modules : le verrou est suivi, le contenu non.
# `npx skills install` les restaure à l'identique depuis skills-lock.json.
.agents/
.claude/skills/

# Captures d’écran de vérification : elles se prennent dans le répertoire
# temporaire de session, jamais dans le dépôt.
*.png
!public/**/*.png
